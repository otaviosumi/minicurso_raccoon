Para ativar:
$ virtualenv -p python3 env
$ source env/bin/activate

Para verificar instalados:
$ pip freeze

Para instalar:
$ pip install mongoengine
$ pip install flask

Para desativar:
$ deactivate

